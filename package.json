{
  "name": "buzz-mcp-bridge",
  "version": "0.1.0",
  "description": "Give Buzz managed agents the MCP servers your Claude Desktop / Claude Code config already has, behind an explicit per-agent allowlist.",
  "type": "module",
  "bin": { "buzz-mcp-bridge": "./index.mjs" },
  "engines": { "node": ">=20" },
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/hachiflow/buzz-mcp-bridge" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
